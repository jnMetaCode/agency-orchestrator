# Agency Orchestrator — NAS/家庭服务器一键部署（#93）
#   docker compose up -d && 打开 http://<主机IP>:8088
# 密钥两种给法（选一即可）：
#   1) 打开页面 →「供应商」里粘贴 key（存进 ao-data 卷，重启不丢）
#   2) 下面 environment 里直接给环境变量
services:
  agency-orchestrator:
    image: ghcr.io/jnmetacode/agency-orchestrator:latest
    container_name: agency-orchestrator
    ports:
      - "8088:8088"
    volumes:
      - ao-data:/data
    environment:
      # 容器监听 0.0.0.0：同一网络里任何人打开就能用你存的 key 跑任务、删运行、改配置。
      # 放在可信内网之外（或家里有别人用同一个网）时，设一个访问令牌：
      # - AO_WEB_TOKEN=换成你自己的一串口令        # 之后用 http://<主机IP>:8088/?token=<口令> 打开一次
      # - DEEPSEEK_API_KEY=sk-xxx
      # - DUOYUANX_API_KEY=sk-xxx
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  ao-data:
